const operations = {
  addition: (a, b) => a + b,
  subtraction: (a, b) => a - b,
  division: (a, b) => a / b,
  multiplication: (a, b) => a * b,
  empowerment: (a, b) => Math.pow(a, b),
  reversSign: (a) => a * -1,
};